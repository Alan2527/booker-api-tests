[pytest]
testpaths = tests
addopts = -v --strict-markers
markers =
    salud: health check de la API
    auth: autenticacion
    crear: creacion de reservas
    leer: consulta de reservas
    actualizar: actualizacion total y parcial
    eliminar: baja de reservas
    contrato: validacion de esquemas JSON
